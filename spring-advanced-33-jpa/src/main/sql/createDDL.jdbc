CREATE TABLE CONTACTO (ID BIGINT NOT NULL, NOMBRE VARCHAR(255), COMERCIAL_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE COMERCIAL (ID BIGINT NOT NULL, NOMBRE VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE CONTACTO ADD CONSTRAINT CNTACTOCOMERCIALID FOREIGN KEY (COMERCIAL_ID) REFERENCES COMERCIAL (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
