CREATE TABLE PERSONASINGLETABLE (ID BIGINT NOT NULL, DTYPE VARCHAR(31), NOMBRE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE EMPLEADOSINGLETABLE (ID BIGINT NOT NULL, SUELDO FLOAT, EMPRESA_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE EMPRESASINGLETABLE (ID BIGINT NOT NULL, PRIMARY KEY (ID))
ALTER TABLE EMPLEADOSINGLETABLE ADD CONSTRAINT MPLDSNGLTABLEMPRSD FOREIGN KEY (EMPRESA_ID) REFERENCES EMPRESASINGLETABLE (ID)
ALTER TABLE EMPLEADOSINGLETABLE ADD CONSTRAINT MPLADOSINGLETABLED FOREIGN KEY (ID) REFERENCES PERSONASINGLETABLE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
